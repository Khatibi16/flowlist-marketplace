{
  "name": "flowlist",
  "version": "1.0.0",
  "description": "FlowList - AI-driven marketplace for retailers to turn excess stock into sales",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["marketplace", "ai", "retail", "ecommerce"],
  "author": "FlowList Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
